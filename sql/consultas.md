# Consultas

Para recuperar datos de una base de datos SQL, necesitamos hacer consultas hacia el, ya que una consulta es la instrucción con el cual podemos obtener la información necesaria.  
1. SELECT y FROM: le dice a SQL en qué tabla buscar y que columnas traer, el resultado de esta consulta será un conjunto bidimensional de filas y columnas